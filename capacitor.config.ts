import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'ng-note-taker',
  webDir: 'dist/ng-note-taker',
  bundledWebRuntime: false
};

export default config;
